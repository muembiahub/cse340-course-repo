<%- include('partials/header') %>

<main>
  <h1><%= title %></h1>
  <ul class="category-list">
    <% categories.forEach(category => { %>
      <li class="loader__element" ><%= category.category_name %></li>
    <% }) %>
  </ul>
</main>

<%- include('partials/footer') %>
